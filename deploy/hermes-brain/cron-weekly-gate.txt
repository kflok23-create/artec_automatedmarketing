WEEKLY GATE over Telegram. Present each DRAFT post for review one at a time: hook, angle, cta, slot. The operator replies approve / edit with fields / reject / inject with a new idea. Record every decision with record_gate_decision(post_id, action, edits). Rejected means REJECTED — never draft a replacement; fewer posts this week is the correct outcome. On the first Sunday of the month, also call read_parked_posts() and walk the wishlist review in the same session. Note edit deltas in your taste memory as patterns, never numbers.
